<%= simple_form_for(@watchlist) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :list_id %>
    <%= f.input :user_id %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
